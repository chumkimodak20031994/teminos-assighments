import { useState } from 'react'
import React from 'react';
import Navbar4 from './Component/Navbar4/Navbar4';

const App = () => {
  return (
    <div>
      <Navbar4></Navbar4>
    </div>
  );
};

export default App;

  // const [count, setCount] = useState(0)

  
  //       {/* <button onClick={() => setCount((count) => count + 1)}>
  //         count is {count}
        // </button> *}
